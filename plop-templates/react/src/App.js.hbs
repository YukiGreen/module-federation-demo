import React from 'react'
import { Router, Route, Switch } from 'react-router-dom'

const App = ({ history }) => {
  return (
    <Router history={history}>
      <Switch>

      </Switch>
    </Router>
  )
}

export default App
